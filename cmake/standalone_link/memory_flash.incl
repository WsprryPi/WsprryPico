/* Pico 2 W: physical 4 MiB. Reserve an 8 KiB access journal, 8 KiB BTstack bank, 16 KiB
 * provisioning, 16 KiB standalone, then the final 4 KiB RP2350-E10
 * boot-workaround sector. */
MEMORY
{
    FLASH(rx) : ORIGIN = 0x10000000, LENGTH = 0x3f3000
}
