# Phase 11.4 G4–G7 execution prompt

Own the remaining lost-response, reconnect, identity and recovery acceptance
cases in the joint Phase 11.4 matrix. Work from clean Pico `f187555` and inspect
the exact production candidate before using it. Preserve all failed attempts.

Use only Pico 2 W serial `0BF4B4AEC9FFB344`, WTP device
`fd6127d11d6aca42a9905fa3fb1bf1d5`, on wspr5. Require the standard
`inhibited-standalone-simulator`, authoritative inactive output, disabled
standalone schedules, healthy storage and matching deployment identity before
admission. Bind fresh firmware, boot, server certificate, client, executable,
source, configuration and UTC evidence. Authenticate the existing
`wsprrypico-0a60df.local` identity with the approved controller credential.

Execute the already authorized bounded tests:

1. **G4:** establish an authenticated owner and CAPS-valid finite job. For LOAD,
   ARM and ABORT separately, prevent delivery of the response on the particular
   test TCP socket. Record the exact request bytes and identifiers, independent
   packet observations and USB state showing the operation took effect. Close
   the impaired connection, authenticate again with the original WTP session,
   and reconcile STATUS or replay the identical request. Prove one job and no
   duplicate execution. Application code merely ignoring a received reply is
   insufficient wire-loss evidence.
2. **G5:** use the real isolated production executable, retain its singleton
   protection, and pause/restore only the authorized installed service after
   checking its configuration and provider output. Induce connection loss and
   bounded DNS and certificate-verification reconnect failures in the isolated
   client context. Observe unknown output, preserved failure history and no
   fresh LOAD/ARM; remove faults and explicitly reconcile original identity.
   Label instrumented boundaries and actual network evidence separately.
3. **G6:** kill only the isolated production process after observed ARM, restart
   an idle new process, and prove it cannot adopt the previous session's work.
   Separately exercise a real Pico boot change and the production identity
   latch. Respect Console reset admission: record rejection while owned, then
   explicitly abort and verify inactive/unowned state before rebooting while
   the disconnected host retains its unresolved history. Test changed-device
   rejection at a controlled boundary if feasible;
   never call that evidence of two distinct physical boards.
4. **G7:** while a verified TLS principal owns the finite job, use the separate
   USB principal to attempt CLAIM, ABORT and RELEASE. Require the specified
   ownership failures and unchanged job. Then apply the authorized Console
   ABORT override and verify output disabled, scheduling suspended, ownership
   released and matching terminal evidence retained.

Use socket/process-local faults, no router, global firewall, DNS configuration,
system trust or adjacent-device changes. Keep credentials, firmware, captures
and private configurations ignored and owner-only. Bound every process and
network operation. On uncertain output stop dependent mutations until an
authoritative observation resolves it. Finally verify baseline saved settings,
schedules, watermark, Wi-Fi, installed service/configuration/binary and provider.

Repair actionable defects within the authorized Pico/Pi scope, using focused
behavioral tests and existing build targets. Perform an adversarial assessment
of implementation, fault validity, identity binding, cleanup, negative controls,
and evidence claims. Close findings and repeat the assessment. Record exact
unclosed gates instead of manufacturing PASS. Update the joint matrix and
results with a private-artifact hash index, review the staged diff, commit and
push each changed repository to origin/devel, verify parity, and report the
remaining-items table. Physical RF qualification remains outside this work.
